# Request a key by emailing skillplus@proton.me.
#
# Server-side only. NOT prefixed with NEXT_PUBLIC_ — that prefix would inline
# the key into the browser bundle, which for a credential is exactly wrong.
SKILLPLUS_API_KEY=skp_replace_me

# Optional — staging or self-hosted deployment.
# SKILLPLUS_BASE_URL=https://skillplus.xyz
